package com.example.abdulohal_huddatugaspertemuan4

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.tooling.preview.Preview
import com.example.abdulohal_huddatugaspertemuan4.ui.theme.AbdulohAlhuddaTugasPertemuan4Theme

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            AbdulohAlhuddaTugasPertemuan4Theme {
                Scaffold( modifier = Modifier.fillMaxSize() ) { innerPadding ->
                    Greeting(
                        name = "Abdulloh Hudda",
                        age = 99,
                        ket = "Android App",
                        address = "sukun bebas",
                        modifier = Modifier.padding(innerPadding)
                    )
                }
            }
        }
    }
}

@Composable
fun Greeting(    name: String,
                 age: Int,
                 ket: String,
                 address: String
                ,modifier: Modifier = Modifier) {
    Text(
        text = "Hello : $name!\nUmur saya : $age tahun\nAku sedang membuat project : $ket\nAlamat : $address",
        modifier = modifier
    )
}

